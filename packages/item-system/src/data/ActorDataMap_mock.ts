// use ../data so path can be resolved when moved to
// the generated folder
import type { ActorData } from "../data/ActorData.ts";

export const ActorDataMap: Record<string, Partial<ActorData>> = {};
