use teleparse::Root;
use wasm_bindgen::prelude::*;
// use skybook_parser::CommandInit;

#[wasm_bindgen]
pub fn parse_script(input: String) -> String {
    todo!()
    // match CommandInit::parse(&input) {
    //     Ok(Some(cmd)) => format!("{:?}", cmd),
    //     Ok(None) => "no command found".to_string(),
    //     Err(e) => format!("error: {:?}", e),
    // }
}

